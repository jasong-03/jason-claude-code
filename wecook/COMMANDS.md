# WeCook Plugin Commands

This document provides a comprehensive reference for all available slash commands in the WeCook plugin.

## 🚀 Project Setup

| Command | Description |
|---------|-------------|
| `/bootstrap` | Bootstrap a new project step by step using the Orchestration Protocol. |
| `/new-project` | Kick off a new feature or project with proper planning and architecture. |
| `/new-task` | Analyze task complexity and create actionable implementation plan. |

## 🧠 Planning & Architecture

| Command | Description |
|---------|-------------|
| `/plan` | Intelligent plan creation. Variants: `/plan:fast`, `/plan:hard`, `/plan:two`, `/plan:cro`. |
| `/brainstorm` | Brainstorm a feature or solution with AI assistance. |
| `/ask` | Answer technical and architectural questions. |
| `/design` | Design UI interfaces. Variants: `/design:ui`. |

## 💻 Development

| Command | Description |
|---------|-------------|
| `/cook` | Implement a feature step-by-step (Research → Plan → Code → Test). |
| `/code` | Start coding and testing based on an existing plan. |
| `/ship-feature` | Plan, implement, and verify a feature end-to-end using specialized sub-agents. |
| `/api` | API development tools. Variants: `/api:new`, `/api:protect`, `/api:test`. |
| `/supabase` | Supabase tools. Variants: `/supabase:types-gen`, `/supabase:edge-function-new`. |
| `/integrate` | Integration helpers. Variants: `/integrate:sepay`, etc. |

## 🐞 Debugging & Fixing

| Command | Description |
|---------|-------------|
| `/debug` | Debug technical issues and provide solutions. |
| `/fix` | Analyze and fix issues via intelligent routing. Variants: `/fix:fast`, `/fix:hard`, `/fix:ui`, `/fix:types`, `/fix:logs`, `/fix:ci`. |

## 🔍 Review & Optimization

| Command | Description |
|---------|-------------|
| `/optimize-perf` | Profile and optimize system performance. |
| `/audit-system` | Perform a comprehensive system audit (architecture, security, performance). |
| `/review` | Scan and analyze the codebase. |
| `/test` | Run tests locally and analyze the summary report. |

## 📚 Documentation

| Command | Description |
|---------|-------------|
| `/docs` | Documentation management. Variants: `/docs:init`, `/docs:update`, `/docs:summarize`. |
| `/doc-sprint` | Update and standardize documentation for a feature or component. |
| `/journal` | Write journal entries for development progress. |

## 🛠️ Utilities

| Command | Description |
|---------|-------------|
| `/git` | Git operations. Variants: `/git:cm` (commit), `/git:cp` (commit push), `/git:pr` (PR). |
| `/scout` | Scout directory contents. Variants: `/scout:ext`. |
| `/watzup` | Review recent changes and project status. |
| `/use-mcp` | Utilize Model Context Protocol (MCP) server tools. |
